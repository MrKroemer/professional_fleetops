/// <reference types="vite/client" />

// Declara os tipos de módulos que o Vite resolve em tempo de build — importar um
// PNG devolve a URL do asset, e sem esta referência o TypeScript não sabe disso.
